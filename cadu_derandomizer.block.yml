id: cadu_derandomizer
label: "CADU derandomizer"
category: '[my blocks]'

inputs:
- id: in
  label: in
  domain: message

outputs:
- id: out
  label: out
  domain: message

templates:
  imports: 'from cadu_derandomizer import CaduDerandomizer'
  make: "CaduDerandomizer()"

file_format: 1
